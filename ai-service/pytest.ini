[pytest]
# Use /tmp for cache to save disk space
cache_dir = /tmp/pytest_cache

# Test discovery
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    --strict-markers
    --tb=short
    -v

# Coverage
[coverage:run]
data_file = /tmp/.coverage
